# BTCE 监控性能诊断报告

> **日期**：2026-07-12  
> **问题**：每天8000轮监控耗时越来越久，从18.3h恶化到19.4h  
> **状态**：已定位根因，已部署第一轮优化

---

## 一、诊断过程

### 1.1 数据来源
- 云服务器日志（7/9~7/12，共4天 ~109MB）
- 腾讯云性能监控 CSV（CPU、内存、磁盘，7/1~7/12）
- 性能报告邮件截图（6/26~7/12，15天趋势）

### 1.2 关键发现

#### 每日8000轮耗时趋势（来自性能报告邮件）

| 日期 | 总耗时 | 平均单轮 | 变化 |
|------|--------|----------|------|
| 6/26~7/7 | 18.3~18.4h | 7.5~7.6s | 稳定 |
| **7/8** | 18.4h | **7.8s** | ⚠️ 拐点 |
| 7/9 | 18.4h | 7.8s | 持平 |
| **7/10** | **18.8h** | **8.1s** | 加速恶化 |
| **7/11** | **19.0h** | **8.2s** | 继续恶化 |
| **7/12** | **19.4h** | **8.4s** | 最差 |

#### 日内退化模式（每1000轮耗时走势）

```
7/9: 8.26→8.24→8.24→8.25→8.28→8.29→8.39→8.37  (较平坦)
7/11: 8.36→8.35→8.40→8.60→8.77→8.77→8.71→8.27  (明显后半段更慢)
```

每天后半段（4000轮后）比前半段慢 2.4%，典型资源积累效应。

### 1.3 根因定位

#### ✅ 排除：内存泄漏
- 每天内存 940MB（重启后）→ 1050MB（高峰），非常稳定
- 无泄漏迹象

#### ✅ 排除：磁盘瓶颈  
- IO 等待时间始终 1.1~1.5ms
- 写流量 800~1500 KB/s，完全健康

#### 🔴 确认：CPU 持续攀升

| 期间 | CPU 利用率 | 单轮耗时 |
|------|-----------|----------|
| 7/1~7/7 | 17~20% | 7.5~7.6s |
| 7/8 | 20~23% (+15%) | 7.8s |
| 7/10 | 21~26% (+30%) | 8.1s |
| 7/12 | 23~28% (+44%) | 8.4s |

**CPU 涨 44%，单轮涨 12%，高度正相关。**

#### CPU 被谁吃掉？
- Chrome headless renderer：持续 **55% CPU**（正常应闲置）
- NapCat QQ：~6%
- YDServer（腾讯云监控）：~6%
- Python 主进程：~0.8%

**Chrome renderer 是唯一的 CPU 大户。**

---

## 二、代码分析

### 2.1 每轮操作（`monitor.py:415` run_monitoring_cycle）

```
1. restart_browser_if_needed()  — 每10轮重启（~3s开销）
2. check_dynamic_changes()      — 浏览器打开B站页面，检测置顶评论
   ├─ new_page()                — 创建新页面
   ├─ page.goto(url)            — 加载完整B站页面
   ├─ wait_for_selector         — 等待评论区渲染
   ├─ scrollBy × 5 + sleep(1s)  — 滚动5次，每次等1秒 = 5秒！
   ├─ query_selector_all        — 遍历找置顶评论
   ├─ inner_html()              — 提取HTML
   └─ page.close()              — 关闭页面
3. get_api_dynamics()           — HTTP API 获取动态列表
4. 比对历史数据、保存
```

### 2.2 最大时间杀手

**`render_comment.py:47-49`**：
```python
for _ in range(5):                         # 5次滚动
    await page.evaluate("window.scrollBy(0, 1000)")
    await asyncio.sleep(1)                 # 每次等1秒 = 5秒
```

这5秒占单轮 8.5s 的 **~60%**。滚动的目的是触发 B站 Web Component 的懒加载，让置顶评论渲染出来。但置顶评论永远在评论区顶部，不需要滚 5000px 那么远。

### 2.4 早期版本对比（E:\biliTop\main6.py）

发现了早期雏形版本，同样有滚动循环但单轮仅 ~3s。对比差异：

| | 早期版 `main6.py` | 当前版 `monitor.py` | 开销差异 |
|------|------|------|------|
| **页面** | 启动时创建，**全程复用** | 每轮 `new_page()` + `close()` | ~3-4s |
| **浏览器重启** | **从不重启** | 每10轮重启（~3s） | 平均 0.3s/轮 |
| **提取内容** | 纯文字 `inner_text()` | 完整HTML + shadow DOM + BS4 | ~1s |
| **滚动** | 5次×1s | 5次×1s（优化后3次×0.5s） | 相同→优化后省3.5s |

**页面复用是早期版快的核心原因。**

---

## 三、已部署的优化

### 3.1 正式版（7/12 22:30）

**文件**：`/root/BTCE2.0/render_comment.py`

```diff
- for _ in range(5):
-     await page.evaluate("window.scrollBy(0, 1000)")
-     await asyncio.sleep(1)
+ for _ in range(3):
+     await page.evaluate("window.scrollBy(0, 1000)")
+     await asyncio.sleep(0.5)
```

**效果**：单轮工作 8.5s → 5.0s（-41%），但 `CHECK_INTERVAL=8` 兜底，实际每轮 8s。

### 3.2 测试实例（7/12 22:40）

**目的**：验证页面复用长期稳定性（早期版没长跑过）。

**位置**：`/root/BTCE2.0-test/`（PM2: `bili-monitor-test`）

**改动汇总**：

| 文件 | 改动 | 说明 |
|------|------|------|
| `config.py` | QQ/邮件/ B站推送全关 | 不影响生产 |
| `config.py` | `BROWSER_RESTART_INTERVAL = 100` | 100轮才重启 |
| `config.py` | `PERFORMANCE_REPORT_CYCLE = 500` | 500轮发报告 |
| `config_qq.py` | `QQ_CALLBACK_ENABLED = False` | 避免端口冲突 |
| `monitor.py` | `__init__` 加 `self.page = None` | 持久化页面 |
| `monitor.py` | `check_dynamic_changes` 页面复用 | 不每轮 new_page/close |
| `monitor.py` | `safe_close_browser` 清理持久页 | 重启时正确清理 |
| `render_comment.py` | 同正式版优化 | 3次×0.5s |

**即时效果**：

| 指标 | 正式版 | 测试版 |
|------|--------|--------|
| 单轮耗时 | ~5s | **~3-4s** |
| 页面模式 | 每轮创建销毁 | 持久复用 |
| 浏览器重启 | 每10轮 | 每100轮 |

---

## 四、云服务器文件变更记录

### 正式版
| 文件 | 操作 | 备份 |
|------|------|------|
| `/root/BTCE2.0/render_comment.py` | 已覆盖 | `render_comment.py.bak.v48` |
| 其他文件 | **未动** | — |

### 测试版（完整项目副本）
| 文件 | 操作 | 备份 |
|------|------|------|
| `/root/BTCE2.0-test/` (整个项目) | 新建 | `/root/BTCE2.0/` 原版未动 |
| `config.py` | 替换为测试配置 | `config_prod.bak` |
| `config_qq.py` | 禁用回调 | 原地修改 |
| `monitor.py` | 页面复用改造 | `monitor.py.bak` |

### 回滚命令
```bash
# 正式版回滚
ssh -i <pem> root@43.139.235.117
cd /root/BTCE2.0
cp render_comment.py.bak.v48 render_comment.py
pm2 restart bili-monitor

# 测试版清理（确认稳定后）
pm2 delete bili-monitor-test
rm -rf /root/BTCE2.0-test
```

---

## 五、明天检查清单

### 5.1 测试实例是否存活
```bash
ssh root@43.139.235.117
pm2 status                    # bili-monitor-test 是否 online，重启次数
```

### 5.2 跑了多少轮
```bash
tail -3 /root/BTCE2.0-test/logs/combined.log
# 看 "第 X 轮" 的数字
```

### 5.3 有没有报错
```bash
tail -30 /root/BTCE2.0-test/logs/error.log
# 重点看有没有 "页面"、"Connection closed"、"内存" 相关
```

### 5.4 正式版优化效果
```bash
# CPU 利用率有没有降（预期 25% → 20%）
# 8000轮什么时候跑完（预期提前 ~1h）
tail -5 /root/BTCE2.0/logs/combined.log
```

### 5.5 判断标准
- **测试实例 0 重启 + 跑满几千轮 + 无页面崩溃** → 页面复用安全，可合入正式版
- **测试实例频繁重启或有页面相关报错** → 页面复用不够稳，回退仅保留滚动优化
- **正式版单轮还在 5s+** → 正常，CHECK_INTERVAL=8 兜底

### 5.6 ⚠️ 重点：`render_comment.py` 滚动优化是否有 bug

正式版和测试版都已应用了滚动优化（5次→3次, sleep 1s→0.5s），明天必须检查：

```bash
# 检查正式版日志中是否有 "未找到置顶评论" 增多
grep "未找到置顶评论" /root/BTCE2.0/logs/combined.log | wc -l

# 检查测试版同样指标
grep "未找到置顶评论" /root/BTCE2.0-test/logs/combined.log | wc -l

# 检查超时是否增多
grep "超时" /root/BTCE2.0/logs/combined.log | wc -l
grep "超时" /root/BTCE2.0-test/logs/combined.log | wc -l
```

**判断标准**：
- `"未找到置顶评论"` 日均 < 10 次 → 正常（即使偶尔因滚动不够没拿到，8秒后下轮重试即可）
- `"超时"` 日均 < 5 次 → 正常
- 对比优化前（可从 `error.log` 历史记录推算基线）无明显增多 → 滚动优化安全
- 如果某项明显增多 → 回滚 `render_comment.py`，恢复 5次×1s

**为什么可能出问题**：B站评论区的 Web Component 懒加载依赖滚动事件触发。如果页面 DOM 变重、网络变慢，3次滚动可能不足以让置顶评论渲染完成，导致 `get_pinned_comment()` 返回 `"未找到置顶评论"`。不过最坏情况只是单轮检测失败，8秒后下轮重试。**不会丢通知，只是可能延迟一轮。**

---

## 六、后续优化路线

| 优先级 | 改动 | 预期单轮 | 前提条件 |
|--------|------|----------|----------|
| 🆗 已做 | 滚动 5→3 + sleep 0.5s | 5.0s | — |
| 🔄 测试中 | 页面复用 + 重启间隔 100 | 3-4s | 测试实例稳定 |
| 📋 待定 | CHECK_INTERVAL 8→6 | 全周期缩短 | 前两项稳定 + B站不风控 |
| 📋 待定 | 早期版纯文字模式（可选） | ~2s | 不需要截图/HTML 时 |

---

## 七、附件（本地）

| 文件 | 说明 |
|------|------|
| `E:\BTCE3.0\server_logs\` | 7/9~7/12 服务器日志 |
| `E:\BTCE3.0\服务器性能监控\` | 腾讯云 CPU/内存/磁盘 CSV |
| `E:\BTCE3.0\analyze_rounds.py` | 每日8000轮分析脚本 |
| `E:\BTCE3.0\analyze_deep.py` | 深度逐段分析脚本 |
| `E:\BTCE3.0\analysis_result.txt` | 轮次分析结果 |
| `E:\BTCE3.0\deep_result.txt` | 深度分析结果 |
| `E:\biliTop\` | 早期雏形版本（3s/轮参考） |
